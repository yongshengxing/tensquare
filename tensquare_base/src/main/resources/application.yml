server:
  port: 9001

spring:
  application:
    name: tensquare-base
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.66.134:3306/tensquare_base?characterEncoding=utf-8
    username: root
    password: root
eureka:
  client:
    service-url:
      defaultZone: http://localhost:6868/eureka
  instance:
    prefer-ip-address: true #配置之后以后在服务器部署的时候可以跨域访问,本地测试可以不加
